spring:
  datasource:
    url: jdbc:mysql://localhost:3302/Hotel-db
    username: abdou
    password: abdou
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ur email
    password: ur password
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
        ssl:
          enable: false  # Disable SSL if you are using STARTTLS
server:
  port: 9090
application:
  file-upload-path: ./upload

